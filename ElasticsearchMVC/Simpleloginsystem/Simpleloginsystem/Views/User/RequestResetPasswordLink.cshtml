@model Simpleloginsystem.Models.UserContext

@{
    ViewBag.Title = "RequestResetPasswordLink";
}

<h2>RequestResetPasswordLink</h2>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
        <div class="editor-label">
         @ViewBag.Message;
        </div>
}
